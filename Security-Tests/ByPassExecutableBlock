In Windows, se l'esecuzione di applicazioni sono bloccate da AppLocker, individuare quali sono le directory dove sono presenti delle eccezioni. Per fare ciò digitare:
```powershell
Get-AppLockerPolicy -Effective -Xml
```

Copiare l'eseguibile all'interno di una delle directory mostrate (che corrisponderebbe ad una directory in whitelist), ed eseguirlo da lì.
